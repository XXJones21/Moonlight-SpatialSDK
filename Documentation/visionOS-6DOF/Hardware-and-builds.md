# Deferred hardware and builds

Authoring machine: Windows, PowerShell. CMake and Python are available.
No build or test has been run, per user direction. Visual Studio developer-shell
availability, GPU/driver, Sunshine/SteamVR versions and installed game profile
must be recorded at the first Windows validation session. Do not infer a missing
compiler installation from cl.exe being absent from ordinary PowerShell PATH.

On Mac record model, Xcode version/SDK, headset model/OS, signing team and gamepad.
UESDK access and recursive submodules are Windows UEVR build prerequisites.
